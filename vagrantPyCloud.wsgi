# If installed in a virtualenv, configure and uncomment these lines
#activate_this = '/path/to/venv/bin/activate_this.py'
#execfile(activate_this, dict(__file__=activate_this))

from vagrantPyCloud import app as application
